# Load Emulator Models

Pre-load the neural network models into memory for faster subsequent
runs.

## Usage

``` r
load_emulator_models(predictor = "both")
```

## Arguments

- predictor:

  Character. Which models to load: "prevalence", "cases", or "both".

## Value

Invisible NULL
