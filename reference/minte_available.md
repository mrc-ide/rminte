# Check if minte Python module is available

Checks if the minte Python package is available and can be loaded. This
will trigger Python initialization if not already done.

## Usage

``` r
minte_available()
```

## Value

Logical indicating if minte is available
