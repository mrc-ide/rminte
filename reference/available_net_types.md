# Get Available Net Types

Returns a character vector of available ITN (Insecticide-Treated Net)
types.

## Usage

``` r
available_net_types()
```

## Value

Character vector of net type names.

## Examples

``` r
if (FALSE) { # \dontrun{
net_types <- available_net_types()
print(net_types)
} # }
```
