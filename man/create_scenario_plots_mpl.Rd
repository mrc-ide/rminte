% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting.R
\name{create_scenario_plots_mpl}
\alias{create_scenario_plots_mpl}
\title{Create Scenario Plots (Matplotlib)}
\usage{
create_scenario_plots_mpl(
  results,
  output_dir = NULL,
  plot_type = "both",
  predictor = NULL,
  window_size = 14L,
  plot_tight = FALSE,
  figsize_combined = c(12, 8),
  figsize_individual = c(10, 6),
  dpi = 300L
)
}
\arguments{
\item{results}{A data frame of results (e.g., from \code{results$prevalence}).}

\item{output_dir}{Character. Directory to save plots. If NULL, plots are
returned but not saved.}

\item{plot_type}{Character. Type of plot: "individual", "combined", or "both".}

\item{predictor}{Character. Predictor type ("prevalence" or "cases").
Auto-detected if NULL.}

\item{window_size}{Integer. Days per timestep. Default 14.}

\item{plot_tight}{Logical. Use tight y-axis scaling. Default FALSE.}

\item{figsize_combined}{Numeric vector of length 2. Figure size for combined plot.}

\item{figsize_individual}{Numeric vector of length 2. Figure size for individual plots.}

\item{dpi}{Integer. DPI for saved figures. Default 300.}
}
\value{
A list of matplotlib figure objects (Python objects). These can be
saved to files but are not directly viewable in R graphics devices.
}
\description{
Python/matplotlib wrapper for creating visualizations from MINTe results.
This function calls the underlying Python \code{minte.create_scenario_plots()}
function and returns matplotlib figure objects.

\strong{Note:} For native R plotting with ggplot2, use \code{\link[=plot_prevalence]{plot_prevalence()}} or
\code{\link[=plot_cases]{plot_cases()}} instead, which provide the same functionality using R's
ggplot2 graphics system.
}
\examples{
\dontrun{
results <- run_minter_scenarios(...)
# Save matplotlib plots to files
plots <- create_scenario_plots_mpl(
  results$prevalence,
  output_dir = "plots/",
  plot_type = "both"
)

# For interactive R plotting, use the native functions instead:
p <- plot_prevalence(results)
print(p)
}

}
\seealso{
\itemize{
\item \code{\link[=plot_prevalence]{plot_prevalence()}} for native R/ggplot2 prevalence plots
\item \code{\link[=plot_cases]{plot_cases()}} for native R/ggplot2 cases plots
}
}
