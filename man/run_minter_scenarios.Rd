% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controller.R
\name{run_minter_scenarios}
\alias{run_minter_scenarios}
\title{Run MINTer Scenarios}
\usage{
run_minter_scenarios(
  scenario_tag = NULL,
  res_use,
  py_only,
  py_pbo,
  py_pyrrole,
  py_ppf,
  prev,
  Q0,
  phi,
  season,
  routine,
  irs,
  irs_future,
  lsm,
  itn_future = NULL,
  net_type_future = NULL,
  predictor = c("prevalence", "cases"),
  benchmark = FALSE
)
}
\arguments{
\item{scenario_tag}{Character vector. Tags/names for each scenario.}

\item{res_use}{Numeric vector. Current resistance level (0-1).}

\item{py_only}{Numeric vector. Pyrethroid-only net coverage (0-1).}

\item{py_pbo}{Numeric vector. PBO net coverage (0-1).}

\item{py_pyrrole}{Numeric vector. Pyrrole net coverage (0-1).}

\item{py_ppf}{Numeric vector. PPF net coverage (0-1).}

\item{prev}{Numeric vector. Current under-5 prevalence (0-1).}

\item{Q0}{Numeric vector. Indoor biting proportion (0-1).}

\item{phi}{Numeric vector. Bednet usage proportion (0-1).}

\item{season}{Numeric vector. Seasonality indicator (0 = perennial, 1 = seasonal).}

\item{routine}{Numeric vector. Routine treatment coverage (0-1).}

\item{irs}{Numeric vector. Current IRS coverage (0-1).}

\item{irs_future}{Numeric vector. Future IRS coverage (0-1).}

\item{lsm}{Numeric vector. LSM (Larval Source Management) coverage (0-1).}

\item{itn_future}{Numeric vector. Future ITN coverage (0-1). Optional.}

\item{net_type_future}{Character vector. Future net type. One of "py_only",
"py_pbo", "py_pyrrole", "py_ppf". Optional.}

\item{predictor}{Character vector. Which predictions to run: "prevalence",
"cases", or both. Default is c("prevalence", "cases").}

\item{benchmark}{Logical. If TRUE, print timing benchmarks. Default FALSE.}
}
\value{
A list of class "minter_results" containing:
\describe{
\item{prevalence}{Data frame of prevalence predictions over time}
\item{cases}{Data frame of clinical case predictions over time}
\item{scenario_meta}{Data frame with per-scenario metadata}
\item{eir_valid}{Logical indicating if EIR is within calibrated range}
\item{benchmarks}{List of timing information (if benchmark=TRUE)}
}
}
\description{
Main entry point for running malaria intervention scenarios using the MINTe
neural network emulator. This function takes intervention parameters and
returns predicted prevalence and clinical cases over time.
}
\examples{
\dontrun{
# Single scenario
results <- run_minter_scenarios(
  scenario_tag = "example",
  res_use = 0.3,
  py_only = 0.4,
  py_pbo = 0.3,
  py_pyrrole = 0.2,
  py_ppf = 0.1,
  prev = 0.25,
  Q0 = 0.92,
  phi = 0.85,
  season = 1,
  routine = 0.1,
  irs = 0.0,
  irs_future = 0.3,
  lsm = 0.0
)

# Access results
head(results$prevalence)
head(results$cases)
}

}
